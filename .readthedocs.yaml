# Read the Docs configuration for AMLGraphX.
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
    rust: "1.86"

sphinx:
  configuration: docs/conf.py
  fail_on_warning: true

python:
  install:
    - method: uv
      command: sync
      groups:
        - docs
